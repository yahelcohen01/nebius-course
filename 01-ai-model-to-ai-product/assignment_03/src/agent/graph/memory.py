"""Episodic (SqliteSaver) + per-user profile persistence."""
from __future__ import annotations

# TODO:
# - build_checkpointer(db_path) -> SqliteSaver
# - load_profile(user_id) / update_profile(user_id, facts)
