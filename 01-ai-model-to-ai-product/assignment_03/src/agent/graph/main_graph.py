"""LangGraph ReAct graph wiring router -> agent -> tools with checkpointed memory."""
from __future__ import annotations

# TODO: assemble StateGraph with router node, agent node (bind_tools), tool node,
#       max-iteration guard, and SqliteSaver checkpointer.
